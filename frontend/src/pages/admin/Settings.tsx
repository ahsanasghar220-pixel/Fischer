// Re-export barrel — the real implementation lives in ./settings/index.tsx
// This file exists so the existing App.tsx import path ('pages/admin/Settings') keeps working.
export { default } from './settings/index'
